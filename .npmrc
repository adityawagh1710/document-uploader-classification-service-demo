# Workspace-wide pnpm config (platform mandates pnpm — tech-environment.md).
# hoisted node_modules keeps tsc/vitest/esbuild/CDK/Next-standalone/Lambda
# resolution flat (like npm), avoiding pnpm symlink surprises in the bundlers.
node-linker=hoisted
